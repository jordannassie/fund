export default function Problem() {
  return (
    <section className="border-b border-zinc-100 bg-zinc-950 px-4 py-6 sm:px-6">
      <div className="mx-auto max-w-6xl">
        <p className="text-xs font-semibold uppercase tracking-[0.4em] text-white/60">THE PROBLEM</p>
        <h2 className="mt-2 text-2xl font-semibold leading-tight text-white sm:text-3xl">
          The greatest barrier to spreading the Gospel is not the mission — it is funding.
        </h2>
        <p className="mt-3 text-sm leading-relaxed text-white/70">
          Without funding, Bibles are not distributed, evangelists are not sent, churches are not
          planted, and unreached people remain unreached.
        </p>
        <p className="mt-2 text-sm font-semibold uppercase tracking-[0.3em] text-white/80">
          We help carry the funding burden so others can stay focused on the mission.
        </p>
      </div>
    </section>
  )
}
